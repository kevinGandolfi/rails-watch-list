<nav class="navbar navbar-expand-lg bg-primary">
  <div class="container">
    <div class="navbar-brand">brand logo</div>
    <ul class="navbar-nav ml-lg-auto">
      <li class="nav-item">
        <a href="#" class="nav-link">My lists</a>
      </li>
      <li class="nav-item">
        <a href="#" class="nav-link">New list</a>
      </li>
    </ul>
  </div>
</nav>

<% @lists.each do |list| %>
<h2><%= link_to list.name, list_path(list) %></h2>

<% end %>
<%= cl_image_tag @lists.last.photo.key, height: 300, width: 400, crop: :fill %>
<%= link_to 'Create a new list', new_list_path %>
